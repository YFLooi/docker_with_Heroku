{
  "name": "docker-with-heroku",
  "version": "1.0.0",
  "description": "Example deployment of React app to Heroku with Docker",
  "main": "server.js",
  "dependencies": {
    "express": "4.17.1",
    "dotenv": "8.2.0"
  },
  "scripts": {
    "heroku-postbuild": "npm install && cd client && npm install && npm run build",
    "postbuild": "cd client && npm start",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "^9.9.0",
    "npm": "^5.6.0"
  },
  "author": "YFLooi",
  "license": "ISC"
}
