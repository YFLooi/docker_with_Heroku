{
  "name": "docker-with-heroku",
  "version": "1.0.0",
  "description": "Example deployment of React app to Heroku with Docker",
  "main": "server.js",
  "dependencies": {
    "body-parser": "^1.20.1",
    "dotenv": "8.2.0",
    "express": "4.18.2",
    "path": "^0.12.7"
  },
  "scripts": {
    "heroku-postbuild": "npm install && cd client && npm install && npm build",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "13.12.0",
    "npm": "^5.6.0"
  },
  "author": "YFLooi",
  "license": "ISC"
}
